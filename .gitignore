# OS
.DS_Store
Thumbs.db

# self-signed TLS cert/key for LAN/mobile HTTPS testing (machine-specific, never commit private keys)
/certs/*.pem
/certs/*.key
/certs/*.crt
/certs/*.cnf

# local dev database and secrets
apps/api/.env
apps/api/prisma/dev.db
apps/api/prisma/dev.db-journal
